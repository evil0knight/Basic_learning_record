
# 项目规范与操作指南

## 简称约定

| 简称 | 对应文件夹     |
| ---- | -------------- |
| 书   | 书中自有黄金屋 |
| 术   | 术中自有万钟粟 |
| 库   | 库中车马多如簇 |

## 新增内容时的更新规则

### 在「书」新增学科

更新两个文件：

1. **`书中自有黄金屋/MOC.md`** — 在对应分类下新增 `- [学科名称](./学科文件夹名/MOC.md)`

### 在「术」新增技术点 / 子分类

1. **`术中自有万钟粟/MOC.md`** — 在技术入口表格新增一行，指向 `.md` 或子文件夹 `MOC.md`

### 在「库」新增模块

适用范围：硬件模块、芯片、器件、PCB电路 与相关原理。

1. **`库中车马多如簇/MOC.md`** — 在模块入口表格新增一行

---

## 文件夹重组规则

当把多个散落的文件合并到一个新文件夹时：

1. `git mv` 搬文件 + 图片文件夹
2. 新建目标文件夹的 `MOC.md`
3. 更新所有被搬出方的 MOC（源文件夹的表格行改路径或删除）
4. 更新所有外部交叉引用（grep 旧路径 → 逐一修正）
5. 搬入方导航栏按新位置更新
6. 最后 grep 确认零残留旧路径

---

## 笔记格式规范

你是一个极其精简的笔记助手。直接切入主题，拒绝寒暄、免责声明、背景介绍和结尾说教。能用要点就不用段落，能用表格就不用文字，信息密度最大化。

### 正文收尾（三区通用）

- **禁止**在正文末尾写"**一句话总结**"、"**本章小结**"等收尾章节。
- 旧笔记里已有的这类章节，修改时顺手删除，正文直接收束。

### 书中自有黄金屋 — 顶部导航

- 学科 `MOC.md`：`[← 学科总览](../MOC.md) | [← 主页](../../index.md)`
- 直接挂在学科 MOC 下的专题正文：`[← <学科名>知识地图](./MOC.md)`
- 专题笔试题页：`[← <学科名>知识地图](./MOC.md) | [← 专题笔记](./对应专题.md)`
- 不要加"上一篇/下一篇/本章题库"等串联导航，除非该页在结构上是某篇上级正文直接展开的子页
- 参考风格：`书中自有黄金屋/操作系统`、`书中自有黄金屋/计算机网络`

### 术中自有万钟粟 — 顶部导航

**所有正文页**（含子文件夹内）必须包含**返回上级 MOC** + **返回主页**：

- 直接挂在分类 MOC 下的页面：`[← 返回 <分类名>](./MOC.md) | [← 主页](../../index.md)`
- 子文件夹内的页面：`[← <子分类名>](./MOC.md) | [← 主页](../../../index.md)`（`../` 层数按深度调整）
- 子展开页：额外追加 `[← 返回 <上级正文标题>](./<上级正文>.md)` 放在最前面
- **MOC.md**：`[← 返回技术栈](../MOC.md) | [← 主页](../../index.md)`

### 库中车马多如簇 — 模块笔记结构

每个模块一个子文件夹，内含：

- **`MOC.md`**：模块基础知识，**不使用 `<details>` 折叠**。顶部放导航表格。
- **程序文件**：严格遵循 SKILL 读取格式（见下方）

#### 程序文件规范（SKILL 读取格式）

```
# <模块名>程序

[← 返回 MOC](./MOC.md) | [← 主页](../../../index.md)

---

## <写法名称>（<适用场景>）

<2-3句说明>

```c
// 完整可复制的代码片段
```

```

**规则：**

1. 每种写法必须有 `## 标题`，标题即 SKILL 选型菜单选项名
2. 代码块用 ` ```c ` 标注语言，内容可直接复制（含必要变量声明）
3. 平台相关函数名保留原样，由用户适配

---

## Quartz 发布注意事项

> ⚠️ 禁止在标签上使用 `style="display:inline"` 属性，构建会报错。
> ⚠️ 所有 Markdown 链接中的文件扩展名必须小写（`.md` 不是 `.md`），Linux 大小写敏感会导致 404。
> ⚠️ 文件夹名和文件名不能含有括号（`()`、`（）`），会被转义导致链接无法打开。

行内公式用 `$...$`，块级 `$$...$$`，不用 `**\mathbf{...}**`。

---

## 上传 GitHub 的操作方式

本项目已通过 SSH 与 GitHub 关联。每次用户说"上传 GitHub"时，
由 Claude 自己依次执行以下命令完成上传：

```bash
git pull
git add .
git commit -m "本次更新了什么"
git push
```

> 说明：commit message 中的"本次更新了什么"应替换为实际更新内容的简短描述。

---

## 其他

- 本地 Quartz 路径：`D:\quartz`
- 友链添加到 index.md 里 `欢迎友链` 下方
